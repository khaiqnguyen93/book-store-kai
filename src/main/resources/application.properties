spring.thymeleaf.cache=false
# ==================
# DATA SOURCE
# ==================

# Config for db connection
spring.datasource.url=jdbc:mysql://us-cdbr-iron-east-05.cleardb.net:3306/heroku_dbedca9af0d9b8a?useSSL=false

# Username and password
spring.datasource.username=b3785dc523e2f8
spring.datasource.password=80d9793c

# Keep the connection alive if idle for a long time
spring.datasource.testWhileIdle=true
spring.datasource.validationQuery=SELECT 1

# ==================
# JPA / HIBERNATE
# ==================

# Show or not log for each sql query
spring.jpa.show-sql=true

# Hibernate auto ddl auto (create, update, drop)
# "Update" - the db schema will be automatically updated accordingly to java entities
# found in the project
spring.jpa.hibernate.ddl-auto = update

# Allows Hibernate to generate SQL optimized for a particular DBMS
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5Dialect

spring.mail.host=smtp.gmail.com
spring.mail.username=khaiqnguyen93test@gmail.com
spring.mail.password=test93khai123
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.socketFactory.port=465
spring.mail.properties.mail.smtp.socketFactory.class=javax.net.ssl.SSLSocketFactory
spring.mail.properties.mail.smtp.socketFactory.fallback=false